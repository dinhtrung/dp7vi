/* ==================== TITLE ELEMENTS =================================== */
.block__title { @extend h4; }
.main-content table { @extend .table; @extend .table-striped; @extend .table-hover; @extend table-responsived; }

/* ==================== BUTTON ELEMENTS =================================== */
.button, .form-submit { @extend .btn; @extend .btn-default; }
#edit-submit { @extend .btn-primary; }
#edit-preview { @extend .btn-info; }
#edit-delete { @extend .btn-danger; }



/* ==================== FORM ELEMENTS =================================== */
.form-item .description { @extend .help-block; }
.form-type-textfield, .form-type-password, .form-type-password-confirm, .form-type-date, 
.form-type-file, 
.form-type-textarea, 
.form-type-select
{ @extend .form-group; }
	.form-text, textarea, .form-select { @extend .form-control; width: auto !important; }
.form-type-checkbox { @extend .checkbox; }
.form-type-radio { @extend .radio; }
.container-inline { 
	@include make-row();
	.form-item & { @include container-fixed(); }
	select { @include make-md-column(3); } 
	.description { @include clearfix(); }
}
// Vertical Tabs
div.vertical-tabs {
	margin: 0 0 15px 0;
}
.vertical-tabs ul.vertical-tabs-list {
	margin: 1px 0 1px 0;
}
.vertical-tabs-panes { @extend .panel-body; }
